<%- include('./header.ejs') %>

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>習う</h1>
    <%  if (i < allCards.length) { %>
    <% if (allCards[i].learned === "false") { %>
      <h1><%= allCards[i].jpWord %></h1>
      
      <form action="/learnSubmit" method="POST">
          <input type="submit" value="習う" required>
      </form>
      <% } else { %>
        <h1>No new words to learn!</h1>
        <% } }else { %>
        <h1>No new words to learn!</h1>
          <% }  %>

  </body>
</html>

